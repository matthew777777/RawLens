package de.hdodenhof.circleimageview;

/** Compatibility values for PhotonCamera's unchanged DngCreator source import. */
public final class BuildConfig {
    // Values exposed by the BuildConfig class accidentally imported by PhotonCamera DngCreator.
    public static final String VERSION_NAME = "";
    public static final int VERSION_CODE = -1;

    private BuildConfig() {}
}
